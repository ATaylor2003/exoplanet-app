---
apiVersion: v1
kind: Service
metadata:
  name: test-flask-api-service
spec:
  selector:
    app: test-flask-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP  # Use NodePort or LoadBalancer for external access
